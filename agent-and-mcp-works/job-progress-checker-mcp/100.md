You're an expert in automating web tasks using Playwright inside Cursor MCP.

🎯 Task:
 check the LogisticsPlatform dashboard for jobs with **100% completion** under the "Delivery in Progress" filter.

---

## 👥 Credentials:
Use both of the following accounts (sequentially):

1. Username: `USER-MANAGER-001`  
   Password: `Password@123`

2. Username: `USER-123456789`  
   Password: `Password@123`

---

## 🔍 Steps (for each user):

1. Visit: https://app.example-platform.com/dashboard  
2. Login using the credentials.
3. Navigate to the **"Job Summary"** section.
4. Click the **"Delivery in Progress"** tab.
5. Select the **"30 Days"** filter.
6. Loop through **all pagination pages**.
7. For each job:
   - Check the **Completion Percentage** field.
   - If it is **100%**, extract:
     - Job Name
     - Job Reference ID
     - Completion %
     - Expected End Time

8. After completing all pages for both users:
   - Print to console all jobs found with 100% completion.
   - Include which user account each job came from.

---

## 🧠 Requirements:

- Use Playwright for browser automation.
- Use proper `await` and `try/catch` for stability.
- Handle page waits, selectors, and pagination.
- Output matching jobs in a clear, readable format in the terminal (table or list).

